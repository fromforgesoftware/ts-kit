# @fromforgesoftware/ts-kit

Framework-agnostic TypeScript toolkit: logger, storage, resource-state, JSON:API
codec + client, i18n, date, errors, http, reactive. Base for vue-kit / angular-kit
and the forge console.

```sh
npm i @fromforgesoftware/ts-kit
```
